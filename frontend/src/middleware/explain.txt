In React, "middleware" usually refers to route guards.

Use Case: Checking if a user is logged in before allowing them to see the /settings page. If they aren't, the middleware redirects them to /login.