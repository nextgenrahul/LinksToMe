// config.ts
const config = {
  activeDir: 'ltr' as 'ltr' | 'rtl',
  activeMode: 'dark' as 'light' | 'dark',
  activeTheme: 'blue_theme',
  activeLayout: 'vertical' as 'vertical' | 'horizontal',
  isCardShadow: true,
  isLayout: 'full' as 'boxed' | 'full',
  isBorderRadius: 8,
  isCollapse: 'icon-only' as 'full-sidebar' | 'icon-only', 
  isLanguage: 'en',
};

export default config;